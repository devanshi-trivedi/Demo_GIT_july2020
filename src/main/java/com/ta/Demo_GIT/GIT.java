package com.ta.Demo_GIT;

public class GIT {
	
	//What is repository? --> place where you can store the project or code.
	
	//what is GIT? ---> version control system for tracing changes in source code during software development.
	
	// in project we can have more than 1 people code can be merged and same can be released
	 //git --version
	
	// cd - Project path -enter
	//dir-enter
	//git init
	//git status
	//git remote add origin - give path from repository-entre
	// git remote -v -entre
	//git config --global user.email "you@example.com"
	//git config --global user.name "your name"
	
	//git add * - entre - git status -- all are in green now
	//git commit -m --- entre
	//git push origin master ---> (git push -F)
	// will ask for user name and passowrd give them - entre

}
